import React from 'react'
import ReactDOM from 'react-dom'
import './index.css'
import App from './App'

ReactDOM.render(
  /*  <React.StrictMode> */ /* ---- to review: dirty workaround for npm carousel class based  */
  <App />,
  /* </React.StrictMode>, */
  document.getElementById('root')
)
